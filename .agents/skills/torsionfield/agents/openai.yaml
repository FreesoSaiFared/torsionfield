interface:
  display_name: "Torsionfield"
  short_description: "Repo-specific patterns and workflows for torsionfield"
  default_prompt: "Use the torsionfield repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true