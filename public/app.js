const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];
const form=q('#network-form');if(form)form.addEventListener('submit',async e=>{e.preventDefault();const out=q('#form-status'),fd=new FormData(form);out.textContent='Registering your interest…';const body={displayName:fd.get('displayName'),email:fd.get('email'),nodeIntent:fd.get('nodeIntent'),interests:fd.getAll('interests'),website:fd.get('website'),accepted:fd.get('accepted')==='on'};try{const r=await fetch('/api/network/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),j=await r.json();if(!r.ok)throw new Error(j.error||'Request failed');out.textContent='Registered for the Torsionfield Network alpha.';form.reset()}catch(err){out.textContent='Registration failed: '+err.message}});
const search=q('#doc-search');if(search)search.addEventListener('input',()=>{const v=search.value.toLowerCase();qa('.side a[data-doc]').forEach(a=>a.classList.toggle('hide',!a.textContent.toLowerCase().includes(v)))});
qa('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{await navigator.clipboard.writeText(b.dataset.copy);b.textContent='Copied'}));
